<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.veltrix">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Veltrix">
        <activity
            android:name=".InstagramInitiation"
            android:exported="false"
            android:label="@string/title_activity_instagram_initiation"
            android:theme="@style/Theme.Veltrix" />
        <activity
            android:name=".XInitiation"
            android:exported="false"
            android:label="@string/title_activity_xinitiation"
            android:theme="@style/Theme.Veltrix" />
        <activity
            android:name=".WhatsappInitiation"
            android:exported="false"
            android:label="@string/title_activity_whatsapp_initiation"
            android:theme="@style/Theme.Veltrix" /> <!-- launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Veltrix">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>